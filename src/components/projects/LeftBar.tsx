import React from 'react';
import styled from 'styled-components';

const BarContainer = styled.div`
  flex: 1.5;
  float: left;
`;

const LeftBar = () => {
  return <BarContainer>test</BarContainer>;
};

export default LeftBar;
